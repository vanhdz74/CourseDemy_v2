version: '3.9'

services:
  redis:
    image: redis:7.2
    container_name: redis-CourseDemy
    restart: always
    ports:
      - "6380:6379"
    volumes:
      - redis_data:/data
    networks:
      - spring-microservice
volumes:
  redis_data:
networks:
  spring-microservice:
    driver: bridge